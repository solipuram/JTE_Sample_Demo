void call ()
{
    stage("Git:Checkout")
    {
        
            echo " Working on Git Stage"
            // git url: "${config.url}", 
            // credentialsId: "${config.crednetialsId}", 
            // branch: "${config.branch}"
        
        
    }
}